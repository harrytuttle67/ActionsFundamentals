name: My awesome first workflow
on:
  push:
    branches:
    - main
    paths-ignore:
    - .github/**
  pull_request:
    branches: 
    - main
  schedule:
    - cron: '15 6 * * 0'
  workflow_dispatch:
    
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The job was triggered by event ${{ github.event_name }}"
      - run: echo "... from branch ${{ github.ref }}"

